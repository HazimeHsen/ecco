// SendWhatsAppMessage.tsx
import { useRouter } from "next/navigation";
import React from "react";
const SendWhatsAppMessage = ({ children }: { children: React.ReactNode }) => {
  const router = useRouter();
};

export default SendWhatsAppMessage;
